<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inkstage.mapper.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="ResultMap" type="com.inkstage.entity.model.User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="nickname" column="nickname" />
        <result property="email" column="email" />
        <result property="emailVerified" column="email_verified" />
        <result property="phone" column="phone" />
        <result property="phoneVerified" column="phone_verified" />
        <result property="avatar" column="avatar" />
        <result property="coverImage" column="cover_image" />
        <result property="signature" column="signature" />
        <result property="gender" column="gender" />
        <result property="birthDate" column="birth_date" />
        <result property="location" column="location" />
        <result property="website" column="website" />
        <result property="followCount" column="follow_count" />
        <result property="followerCount" column="follower_count" />
        <result property="articleCount" column="article_count" />
        <result property="commentCount" column="comment_count" />
        <result property="likeCount" column="like_count" />
        <result property="lastLoginTime" column="last_login_time" />
        <result property="lastLoginIp" column="last_login_ip" />
        <result property="registerIp" column="register_ip" />
        <result property="registerTime" column="register_time" />
        <result property="privacy" column="privacy" />
        <result property="status" column="status" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
        <result property="deleted" column="deleted" />
    </resultMap>

    <sql id="columns">
        id, username, password, nickname, email, email_verified, phone, phone_verified, avatar, cover_image, signature,
        gender, birth_date, location, website, follow_count, follower_count, article_count, comment_count, like_count,
        last_login_time, last_login_ip, register_ip, register_time, privacy, status, create_time, update_time, deleted
    </sql>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultMap="ResultMap">
        SELECT <include refid="columns"/> FROM user WHERE username = #{username} AND deleted = 0
    </select>

    <!-- 根据邮箱查询用户 -->
    <select id="selectByEmail" resultMap="ResultMap">
        SELECT <include refid="columns"/> FROM user WHERE email = #{email} AND deleted = 0
    </select>

    <!-- 根据手机号查询用户 -->
    <select id="selectByPhone" resultMap="ResultMap">
        SELECT <include refid="columns"/> FROM user WHERE phone = #{phone} AND deleted = 0
    </select>

    <!-- 新增用户 -->
    <insert id="insert" parameterType="com.inkstage.entity.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (
            username, password, nickname, email, email_verified,
            phone, phone_verified, avatar, cover_image, signature,
            gender, birth_date, location, website, follow_count,
            follower_count, article_count, comment_count, like_count,
            last_login_time, last_login_ip, register_ip, register_time,
            privacy, status, create_time, update_time, deleted
        ) VALUES (
            #{username}, #{password}, #{nickname}, #{email}, #{emailVerified},
            #{phone}, #{phoneVerified}, #{avatar}, #{coverImage}, #{signature},
            #{gender}, #{birthDate}, #{location}, #{website}, #{followCount},
            #{followerCount}, #{articleCount}, #{commentCount}, #{likeCount},
            #{lastLoginTime}, #{lastLoginIp}, #{registerIp}, #{registerTime},
            #{privacy}, #{status}, #{createTime}, #{updateTime}, #{deleted}
        )
    </insert>

    <!-- 根据主键更新用户信息（选择性更新） -->
    <update id="updateByPrimaryKeySelective" parameterType="com.inkstage.entity.model.User">
        UPDATE user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="email != null">email = #{email},</if>
            <if test="emailVerified != null">email_verified = #{emailVerified},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="phoneVerified != null">phone_verified = #{phoneVerified},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="coverImage != null">cover_image = #{coverImage},</if>
            <if test="signature != null">signature = #{signature},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="birthDate != null">birth_date = #{birthDate},</if>
            <if test="location != null">location = #{location},</if>
            <if test="website != null">website = #{website},</if>
            <if test="followCount != null">follow_count = #{followCount},</if>
            <if test="followerCount != null">follower_count = #{followerCount},</if>
            <if test="articleCount != null">article_count = #{articleCount},</if>
            <if test="commentCount != null">comment_count = #{commentCount},</if>
            <if test="likeCount != null">like_count = #{likeCount},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            <if test="lastLoginIp != null">last_login_ip = #{lastLoginIp},</if>
            <if test="registerIp != null">register_ip = #{registerIp},</if>
            <if test="registerTime != null">register_time = #{registerTime},</if>
            <if test="privacy != null">privacy = #{privacy},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="deleted != null">deleted = #{deleted},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据主键查询用户 -->
    <select id="selectByPrimaryKey" resultMap="ResultMap">
        SELECT <include refid="columns"/> FROM user WHERE id = #{id} AND deleted = 0
    </select>
</mapper>