<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.inkstage.mapper.ArticleMapper">
    <!-- 基础结果映射 -->
    <resultMap id="ResultMap" type="com.inkstage.entity.model.Article">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="summary" property="summary"/>
        <result column="content" property="content"/>
        <result column="content_html" property="contentHtml"/>
        <result column="cover_image" property="coverImage"/>
        <result column="user_id" property="userId"/>
        <result column="author_name" property="authorName"/>
        <result column="category_id" property="categoryId"/>
        <result column="article_status" property="articleStatus" javaType="com.inkstage.enums.article.ArticleStatus"/>
        <result column="review_status" property="reviewStatus" javaType="com.inkstage.enums.ReviewStatus"/>
        <result column="publish_time" property="publishTime"/>
        <result column="read_count" property="readCount"/>
        <result column="like_count" property="likeCount"/>
        <result column="comment_count" property="commentCount"/>
        <result column="collection_count" property="collectionCount"/>
        <result column="share_count" property="shareCount"/>
        <result column="top" property="top" javaType="com.inkstage.enums.article.TopStatus"/>
        <result column="recommended" property="recommended"
                javaType="com.inkstage.enums.article.RecommendStatus"/>
        <result column="visible" property="visible" javaType="com.inkstage.enums.VisibleStatus"/>
        <result column="allow_comment" property="allowComment"
                javaType="com.inkstage.enums.AllowStatus"/>
        <result column="allow_forward" property="allowForward"
                javaType="com.inkstage.enums.AllowStatus"/>
        <result column="original" property="original" javaType="com.inkstage.enums.article.OriginalStatus"/>
        <result column="original_url" property="originalUrl"/>
        <result column="last_edit_time" property="lastEditTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted" javaType="com.inkstage.enums.DeleteStatus"/>
        <result column="deleted_time" property="deletedTime"/>
    </resultMap>

    <sql id="baseColumns">
        id, title, summary, content, content_html, cover_image, user_id, author_name, category_id, article_status, review_status, publish_time,
        read_count, like_count, comment_count, collection_count, share_count, top, recommended, visible, allow_comment,
        allow_forward, original, original_url, last_edit_time,create_time, update_time, deleted, deleted_time
    </sql>

    <select id="selectById" resultMap="ResultMap">
        select
        <include refid="baseColumns"/>
        from article where id = #{id} and deleted = 0
    </select>

    <insert id="insert" parameterType="com.inkstage.entity.model.Article" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO article
        (title, summary, content, content_html, cover_image, user_id, author_name, category_id, article_status, review_status, publish_time,
        read_count, like_count, comment_count, collection_count, share_count, top, recommended, visible, allow_comment,
        allow_forward, original, original_url, last_edit_time,create_time, update_time, deleted, deleted_time)
        VALUES
        (#{title}, #{summary}, #{content}, #{contentHtml}, #{coverImage}, #{userId}, #{authorName}, #{categoryId},
        #{status}, #{reviewStatus}, #{publishTime},
        #{readCount}, #{likeCount}, #{commentCount}, #{collectionCount}, #{shareCount}, #{top}, #{recommended},
        #{visible}, #{allowComment},
        #{allowForward}, #{original}, #{originalUrl}, #{lastEditTime}, #{createTime}, #{updateTime}, #{deleted},
        #{deletedTime})
    </insert>

    <update id="update" parameterType="com.inkstage.entity.model.Article">
        update article
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="summary != null">summary = #{summary},</if>
            <if test="content != null">content = #{content},</if>
            <if test="contentHtml != null">content_html = #{contentHtml},</if>
            <if test="coverImage != null">cover_image = #{coverImage},</if>
            <if test="authorName != null">author_name = #{authorName},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="articleStatus != null">article_status = #{articleStatus},</if>
            <if test="reviewStatus != null">review_status = #{reviewStatus},</if>
            <if test="publishTime != null">publish_time = #{publishTime},</if>
            <if test="readCount != null">read_count = #{readCount},</if>
            <if test="likeCount != null">like_count = #{likeCount},</if>
            <if test="commentCount != null">comment_count = #{commentCount},</if>
            <if test="collectionCount != null">collection_count = #{collectionCount},</if>
            <if test="shareCount != null">share_count = #{shareCount},</if>
            <if test="top != null">top = #{top},</if>
            <if test="recommended != null">recommended = #{recommended},</if>
            <if test="visible != null">visible = #{visible},</if>
            <if test="allowComment != null">allow_comment = #{allowComment},</if>
            <if test="allowForward != null">allow_forward = #{allowForward},</if>
            <if test="original != null">original = #{original},</if>
            <if test="originalUrl != null">original_url = #{originalUrl},</if>
            <if test="lastEditTime != null">last_edit_time = #{lastEditTime},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="deleted != null">deleted = #{deleted},</if>
            <if test="deletedTime != null">deleted_time = #{deletedTime}</if>
        </set>
        where id = #{id}
    </update>

    <!--逻辑删除文章 - 移至回收站 -->
    <update id="deleteById">
        update article set status = 5 where id = #{id} and user_id = #{userId}
    </update>

    <delete id="permanentDeleteById">
        update article set deleted = 1 where id = #{id}
    </delete>


    <!-- 文章列表结果映射 -->
    <resultMap id="ArticleListResultMap" type="com.inkstage.vo.front.ArticleListVO">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="summary" property="summary"/>
        <result column="cover_image" property="coverImage"/>
        <result column="article_status" property="articleStatus"/>
        <result column="visible" property="visible"/>
        <result column="publish_time" property="publishTime"/>
        <result column="last_edit_time" property="lastEditTime"/>
        <result column="read_count" property="readCount"/>
        <result column="like_count" property="likeCount"/>
        <result column="comment_count" property="commentCount"/>
        <!-- 作者信息 -->
        <result column="user_id" property="userId"/>
        <result column="author_name" property="authorName"/>
        <result column="avatar" property="avatar"/>
        <!-- 分类信息 -->
        <result column="category_id" property="categoryId"/>
        <result column="category_name" property="categoryName"/>
    </resultMap>

    <!-- 文章列表查询 -->
    <select id="selectArticleList" resultMap="ArticleListResultMap">
        SELECT
        a.id,
        a.title,
        a.summary,
        a.cover_image,
        a.article_status,
        a.visible,
        a.publish_time,
        a.last_edit_time,
        a.read_count,
        a.like_count,
        a.comment_count,
        a.user_id,
        a.author_name,
        a.category_id,
        u.avatar,
        c.name as category_name
        FROM
        article a
        LEFT JOIN
        category c ON a.category_id = c.id
        LEFT JOIN
        user u ON a.user_id = u.id
        WHERE
        a.deleted = 0
        <if test="query.categoryId != null and query.categoryId != 0">
            AND a.category_id = #{query.categoryId}
        </if>
        <if test="query.keyword != null and query.keyword != ''">
            AND (a.title LIKE CONCAT('%', #{query.keyword}, '%') OR a.summary LIKE CONCAT('%', #{query.keyword}, '%'))
        </if>
        GROUP BY
        a.id
        ORDER BY
        a.top DESC,
        a.recommended DESC,
        a.create_time DESC
        LIMIT #{query.offset}, #{query.pageSize}
    </select>

    <!-- 文章总数查询 -->
    <select id="countArticleList" resultType="java.lang.Long">
        SELECT
        COUNT(DISTINCT a.id)
        FROM
        article a
        LEFT JOIN
        category c ON a.category_id = c.id
        WHERE
        a.deleted = 0
        <if test="query.categoryId != null and query.categoryId != 0">
            AND a.category_id = #{query.categoryId}
        </if>
        <if test="query.keyword != null and query.keyword != ''">
            AND (a.title LIKE CONCAT('%', #{query.keyword}, '%') OR a.summary LIKE CONCAT('%', #{query.keyword}, '%'))
        </if>
    </select>


    <select id="selectDetailById" resultType="com.inkstage.vo.front.ArticleDetailVO">
        SELECT
        a.id,
        a.title,
        a.content,
        a.content_html,
        a.summary,
        a.cover_image,
        a.article_status,
        a.visible,
        a.allow_comment,
        a.allow_forward,
        a.original,
        a.original_url,
        a.publish_time,
        a.last_edit_time,
        a.read_count,
        a.like_count,
        a.comment_count,
        a.collection_count,
        a.share_count,
        a.user_id as user_id,
        a.author_name,
        u.avatar,
        u.article_count,
        u.follow_count,
        u.follower_count,
        a.category_id,
        c.name as category_name
        FROM article a
        LEFT JOIN user u ON a.user_id = u.id
        LEFT JOIN category c ON a.category_id = c.id
        WHERE a.id = #{id} AND a.deleted = 0
    </select>

    <!-- 热门文章查询 -->
    <select id="selectHotArticles" resultMap="ArticleListResultMap">
        SELECT
        a.id,
        a.title,
        a.summary,
        a.cover_image,
        a.article_status,
        a.visible,
        a.publish_time,
        a.last_edit_time,
        a.read_count,
        a.like_count,
        a.comment_count,
        a.user_id,
        a.author_name,
        a.category_id,
        u.avatar,
        c.name as category_name
        FROM
        article a
        LEFT JOIN
        category c ON a.category_id = c.id
        LEFT JOIN
        user u ON a.user_id = u.id
        WHERE
        a.deleted = 0
        AND a.article_status = 3 <!-- 已发布 -->
        ORDER BY
        a.read_count DESC,
        a.like_count DESC,
        a.comment_count DESC
        LIMIT #{limit}
    </select>

    <!-- 最新文章查询 -->
    <select id="selectLatestArticles" resultMap="ArticleListResultMap">
        SELECT
        a.id,
        a.title,
        a.summary,
        a.cover_image,
        a.article_status,
        a.visible,
        a.publish_time,
        a.last_edit_time,
        a.read_count,
        a.like_count,
        a.comment_count,
        a.user_id,
        a.author_name,
        a.category_id,
        u.avatar,
        c.name as category_name
        FROM
        article a
        LEFT JOIN
        category c ON a.category_id = c.id
        LEFT JOIN
        user u ON a.user_id = u.id
        WHERE
        a.deleted = 0
        AND a.article_status = 3 <!-- 已发布 -->
        ORDER BY
        a.publish_time DESC
        LIMIT #{limit}
    </select>

    <!-- 轮播图文章查询 -->
    <select id="selectBannerArticles" resultMap="ArticleListResultMap">
        SELECT
        a.id,
        a.title,
        a.summary,
        a.cover_image,
        a.article_status,
        a.visible,
        a.publish_time,
        a.last_edit_time,
        a.read_count,
        a.like_count,
        a.comment_count,
        a.user_id,
        a.author_name,
        a.category_id,
        u.avatar,
        c.name as category_name
        FROM
        article a
        LEFT JOIN
        category c ON a.category_id = c.id
        LEFT JOIN
        user u ON a.user_id = u.id
        WHERE
        a.deleted = 0
        AND a.article_status = 3 <!-- 已发布 -->
        AND a.cover_image IS NOT NULL AND a.cover_image != '' <!-- 有封面图片 -->
        ORDER BY
        a.top DESC,
        a.recommended DESC,
        a.read_count DESC,
        a.publish_time DESC
        LIMIT #{limit}
    </select>

    <!-- 用户文章列表查询 -->
    <select id="selectUserArticles" resultMap="ArticleListResultMap">
        SELECT
        a.id,
        a.title,
        a.summary,
        a.cover_image,
        a.article_status,
        a.visible,
        a.publish_time,
        a.last_edit_time,
        a.read_count,
        a.like_count,
        a.comment_count,
        a.user_id,
        a.author_name,
        a.category_id,
        u.avatar,
        c.name as category_name
        FROM
        article a
        LEFT JOIN
        category c ON a.category_id = c.id
        LEFT JOIN
        user u ON a.user_id = u.id
        WHERE
        a.deleted = 0
        AND a.user_id = #{userId}
        AND a.article_status = 3 <!-- 已发布 -->
        ORDER BY
        a.top DESC,
        a.publish_time DESC
        LIMIT #{offset}, #{size}
    </select>

    <!-- 用户文章总数查询 -->
    <select id="countUserArticles" resultType="java.lang.Long">
        SELECT
        COUNT(DISTINCT a.id)
        FROM
        article a
        WHERE
        a.deleted = 0
        AND a.user_id = #{userId}
        AND a.article_status = 3 <!-- 已发布 -->
    </select>

    <!-- 作者相关文章查询 -->
    <select id="selectAuthorRelatedArticles" resultMap="ArticleListResultMap">
        SELECT
        a.id,
        a.title,
        a.summary,
        a.cover_image,
        a.article_status,
        a.visible,
        a.publish_time,
        a.last_edit_time,
        a.read_count,
        a.like_count,
        a.comment_count,
        a.user_id,
        a.author_name,
        a.category_id,
        u.avatar,
        c.name as category_name
        FROM
        article a
        LEFT JOIN
        category c ON a.category_id = c.id
        LEFT JOIN
        user u ON a.user_id = u.id
        WHERE
        a.deleted = 0
        AND a.user_id = #{userId}
        AND a.id != #{excludeArticleId}
        AND a.article_status = 3 <!-- 已发布 -->
        ORDER BY
        a.top DESC,
        a.publish_time DESC
        LIMIT #{limit}
    </select>

    <!-- 文章列表结果映射 -->
    <resultMap id="MyArticleListResultMap" type="com.inkstage.vo.front.MyArticleListVO">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="summary" property="summary"/>
        <result column="article_status" property="articleStatus" javaType="com.inkstage.enums.article.ArticleStatus"/>
        <result column="review_status" property="reviewStatus" javaType="com.inkstage.enums.ReviewStatus"/>
        <result column="visible" property="visible"/>
        <result column="original" property="original" javaType="com.inkstage.enums.article.OriginalStatus"/>
        <result column="publish_time" property="publishTime"/>
        <result column="read_count" property="readCount"/>
        <result column="like_count" property="likeCount"/>
        <result column="comment_count" property="commentCount"/>
        <!-- 作者信息 -->
        <result column="user_id" property="userId"/>
    </resultMap>

    <!-- 管理员文章列表结果映射 -->
    <resultMap id="AdminArticleResultMap" type="com.inkstage.vo.admin.AdminArticleVO">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="author_name" property="authorName"/>
        <result column="category_name" property="categoryName"/>
        <result column="article_status" property="articleStatus" javaType="com.inkstage.enums.article.ArticleStatus"/>
        <result column="publish_time" property="publishTime"/>
        <result column="read_count" property="readCount"/>
        <result column="like_count" property="likeCount"/>
        <result column="comment_count" property="commentCount"/>
        <result column="top" property="top" javaType="com.inkstage.enums.article.TopStatus"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <!-- 标签列表 -->
        <collection property="tags" ofType="java.lang.String"/>
    </resultMap>
    <!-- 当前用户文章列表查询 -->
    <select id="selectMyArticles" resultMap="MyArticleListResultMap">
        SELECT
        id,
        title,
        summary,
        user_id,
        article_status,
        review_status,
        visible,
        original,
        publish_time,
        read_count,
        like_count,
        comment_count
        FROM
        article
        WHERE
        deleted = 0
        AND user_id = #{userId}
        <if test="articleStatus != null and articleStatus.code != 0">
            AND article_status = #{articleStatus.code}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%') OR summary LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY
        create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 当前用户文章总数查询 -->
    <select id="countMyArticles" resultType="java.lang.Long">
        SELECT
        COUNT(DISTINCT a.id)
        FROM
        article a
        WHERE
        a.deleted = 0
        AND a.user_id = #{userId}
        <if test="articleStatus != null and articleStatus.code != 0">
            AND article_status = #{articleStatus.code}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (a.title LIKE CONCAT('%', #{keyword}, '%') OR a.summary LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </select>

    <!-- 更新文章阅读数 -->
    <update id="updateReadCount">
        UPDATE article SET read_count = read_count + #{increment} WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 更新文章点赞数 -->
    <update id="updateLikeCount">
        UPDATE article SET like_count = like_count + #{offset} WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 更新文章评论数 -->
    <update id="updateCommentCount">
        UPDATE article SET comment_count = comment_count + #{offset} WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 更新文章收藏数 -->
    <update id="updateCollectionCount">
        UPDATE article SET collection_count = collection_count + #{offset} WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 更新文章分享数 -->
    <update id="updateShareCount">
        UPDATE article SET share_count = share_count + #{offset} WHERE id = #{id} AND deleted = 0
    </update>

    <select id="getReadCount" resultType="java.lang.Long">
        SELECT read_count FROM article WHERE id = #{articleId} AND deleted = 0
    </select>

    <select id="getLikeCount" resultType="java.lang.Long">
        SELECT like_count FROM article WHERE id = #{articleId} AND deleted = 0
    </select>

    <select id="getCommentCount" resultType="java.lang.Long">
        SELECT comment_count FROM article WHERE id = #{articleId} AND deleted = 0
    </select>

    <select id="getCollectionCount" resultType="java.lang.Long">
        SELECT collection_count FROM article WHERE id = #{articleId} AND deleted = 0
    </select>

    <select id="getShareCount" resultType="java.lang.Long">
        SELECT share_count FROM article WHERE id = #{articleId} AND deleted = 0
    </select>

    <!-- 搜索文章查询 -->
    <select id="searchArticles" resultMap="ArticleListResultMap">
        SELECT
        a.id,
        a.title,
        a.summary,
        a.cover_image,
        a.article_status,
        a.visible,
        a.publish_time,
        a.last_edit_time,
        a.read_count,
        a.like_count,
        a.comment_count,
        a.user_id,
        a.author_name,
        a.category_id,
        u.avatar,
        c.name as category_name
        FROM
        article a
        LEFT JOIN
        category c ON a.category_id = c.id
        LEFT JOIN
        user u ON a.user_id = u.id
        WHERE
        a.deleted = 0
        AND a.article_status = 3 <!-- 已发布 -->
        AND (a.title LIKE CONCAT('%', #{keyword}, '%') OR a.summary LIKE CONCAT('%', #{keyword}, '%') OR a.content LIKE CONCAT('%', #{keyword}, '%'))
        <choose>
            <when test="sortBy == 'relevance'">
                ORDER BY
                CASE
                    WHEN a.title LIKE CONCAT(#{keyword}, '%') THEN 0
                    WHEN a.title LIKE CONCAT('%', #{keyword}, '%') THEN 1
                    WHEN a.summary LIKE CONCAT(#{keyword}, '%') THEN 2
                    WHEN a.summary LIKE CONCAT('%', #{keyword}, '%') THEN 3
                    ELSE 4
                END,
                a.read_count DESC,
                a.publish_time DESC
            </when>
            <when test="sortBy == 'publishTime'">
                ORDER BY
                a.publish_time DESC
            </when>
            <when test="sortBy == 'readCount'">
                ORDER BY
                a.read_count DESC,
                a.publish_time DESC
            </when>
            <otherwise>
                ORDER BY
                a.read_count DESC,
                a.publish_time DESC
            </otherwise>
        </choose>
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 搜索文章总数查询 -->
    <select id="countSearchArticles" resultType="java.lang.Long">
        SELECT
        COUNT(DISTINCT a.id)
        FROM
        article a
        WHERE
        a.deleted = 0
        AND a.article_status = 3 <!-- 已发布 -->
        AND (a.title LIKE CONCAT('%', #{keyword}, '%') OR a.summary LIKE CONCAT('%', #{keyword}, '%') OR a.content LIKE CONCAT('%', #{keyword}, '%'))
    </select>

    <!-- 管理员文章列表查询 -->
    <select id="selectByPage" resultMap="ResultMap">
        SELECT
        <include refid="baseColumns"/>
        FROM
        article a
        WHERE
        a.deleted = 0
        <if test="query.keyword != null and query.keyword != ''">
            AND (a.title LIKE CONCAT('%', #{query.keyword}, '%') OR a.summary LIKE CONCAT('%', #{query.keyword}, '%'))
        </if>
        <if test="query.categoryId != null and query.categoryId != 0">
            AND a.category_id = #{query.categoryId}
        </if>
        <if test="query.articleStatus != null">
            AND a.article_status = #{query.articleStatus}
        </if>
        ORDER BY
        a.create_time DESC
        LIMIT #{query.offset}, #{query.pageSize}
    </select>

    <!-- 管理员文章总数查询 -->
    <select id="countByPage" resultType="java.lang.Long">
        SELECT
        COUNT(DISTINCT a.id)
        FROM
        article a
        WHERE
        a.deleted = 0
        <if test="query.keyword != null and query.keyword != ''">
            AND (a.title LIKE CONCAT('%', #{query.keyword}, '%') OR a.summary LIKE CONCAT('%', #{query.keyword}, '%'))
        </if>
        <if test="query.categoryId != null and query.categoryId != 0">
            AND a.category_id = #{query.categoryId}
        </if>
        <if test="query.articleStatus != null">
            AND a.article_status = #{query.articleStatus}
        </if>
    </select>

    <!-- 管理员文章列表查询（返回ArticleListVO） -->
    <select id="selectArticlesByPage" resultMap="ArticleListResultMap">
        SELECT
        a.id,
        a.title,
        a.summary,
        a.cover_image,
        a.article_status,
        a.visible,
        a.publish_time,
        a.last_edit_time,
        a.read_count,
        a.like_count,
        a.comment_count,
        a.user_id,
        a.author_name,
        a.category_id,
        u.avatar,
        c.name as category_name
        FROM
        article a
        LEFT JOIN
        category c ON a.category_id = c.id
        LEFT JOIN
        user u ON a.user_id = u.id
        WHERE
        a.deleted = 0
        <if test="query.keyword != null and query.keyword != ''">
            AND (a.title LIKE CONCAT('%', #{query.keyword}, '%') OR a.summary LIKE CONCAT('%', #{query.keyword}, '%'))
        </if>
        <if test="query.categoryId != null and query.categoryId != 0">
            AND a.category_id = #{query.categoryId}
        </if>
        <if test="query.articleStatus != null">
            AND a.article_status = #{query.articleStatus}
        </if>
        ORDER BY
        a.create_time DESC
        LIMIT #{query.offset}, #{query.pageSize}
    </select>

    <!-- 管理员文章总数查询 -->
    <select id="countArticlesByPage" resultType="java.lang.Long">
        SELECT
        COUNT(DISTINCT a.id)
        FROM
        article a
        LEFT JOIN
        category c ON a.category_id = c.id
        WHERE
        a.deleted = 0
        <if test="query.keyword != null and query.keyword != ''">
            AND (a.title LIKE CONCAT('%', #{query.keyword}, '%') OR a.summary LIKE CONCAT('%', #{query.keyword}, '%'))
        </if>
        <if test="query.categoryId != null and query.categoryId != 0">
            AND a.category_id = #{query.categoryId}
        </if>
        <if test="query.articleStatus != null">
            AND a.article_status = #{query.articleStatus}
        </if>
    </select>

    <!-- 更新文章状态 -->
    <update id="updateArticleStatus">
        UPDATE article 
        SET article_status = #{status} 
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 管理员文章列表查询（返回AdminArticleVO） -->
    <select id="selectAdminArticlesByPage" resultMap="AdminArticleResultMap">
        SELECT
        a.id,
        a.title,
        a.author_name,
        a.article_status,
        a.publish_time,
        a.read_count,
        a.like_count,
        a.comment_count,
        a.top,
        a.create_time,
        a.update_time,
        c.name as category_name
        FROM
        article a
        LEFT JOIN
        category c ON a.category_id = c.id
        WHERE
        a.deleted = 0
        <if test="query.keyword != null and query.keyword != ''">
            AND (a.title LIKE CONCAT('%', #{query.keyword}, '%') OR a.summary LIKE CONCAT('%', #{query.keyword}, '%'))
        </if>
        <if test="query.categoryId != null and query.categoryId != 0">
            AND a.category_id = #{query.categoryId}
        </if>
        <if test="query.articleStatus != null">
            AND a.article_status = #{query.articleStatus}
        </if>
        ORDER BY
        a.top DESC,
        a.create_time DESC
        LIMIT #{query.offset}, #{query.pageSize}
    </select>


</mapper>